# Frontend Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# API Configuration (TS backend on port 3000; Vite proxies /api automatically)
# For local dev: leave unset or use /api (Vite proxy handles it)
# For production cross-origin: set to full backend URL, e.g. https://api.yourdomain.com
VITE_API_URL=/api
# For local dev: leave unset or use /ws (Vite proxy handles WebSocket upgrades)
# For production cross-origin: set to full WebSocket URL, e.g. wss://api.yourdomain.com/ws
VITE_WS_URL=/ws

# Supabase (optional — enables auth features)
# Get these from: Supabase Dashboard → Project Settings → API
# VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Feature Flags
VITE_ENABLE_AI_INTERPRETATIONS=false
VITE_ENABLE_USER_AUTH=false

# UI Configuration
VITE_APP_TITLE=Divination Engine
VITE_APP_DESCRIPTION=Online Tarot Card Reading Application

# Optional: Analytics (if needed)
# VITE_GOOGLE_ANALYTICS_ID=your_ga_id_here
