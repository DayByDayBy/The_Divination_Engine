# Database connection string - choose based on your setup:
# Set DB_URL according to your platform.
# Docker Desktop (Mac/Windows): jdbc:postgresql://host.docker.internal:5432/divination
# Linux Docker:                  jdbc:postgresql://localhost:5432/divination
# Native/local:                  jdbc:postgresql://localhost:5432/divination
DB_URL=<your_jdbc_url>

# PostgreSQL username (placeholder – set real value only in your private .env)
DB_USERNAME=your_username

# PostgreSQL password (placeholder – set real value only in your private .env or secret manager)
DB_PASSWORD=your_password

# JDBC driver class - usually org.postgresql.Driver for PostgreSQL
DB_DRIVER=org.postgresql.Driver

# Spring profile - use 'dev' for development, 'prod' for production
SPRING_PROFILE=dev

# Backend server port - default 8080, change if port conflicts
SERVER_PORT=8080

# Frontend API URL - http://localhost:8080/api for native, http://backend:8080/api for Docker
VITE_API_URL=http://localhost:8080/api
