# =============================================================================
# Divination API — Local Environment
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.local.example .env.local

# -----------------------------------------------------------------------------
# Database (Supabase PostgreSQL) — REQUIRED
# -----------------------------------------------------------------------------
# Get these from: Supabase Dashboard → Project Settings → Database
#
# DATABASE_URL  — pooled connection (used at runtime)
# DIRECT_URL    — direct connection (used by prisma db push / migrate)
#
# Replace YOUR_PASSWORD and YOUR_PROJECT_REF below.

DATABASE_URL="postgresql://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@aws-0-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@aws-0-eu-west-1.pooler.supabase.com:5432/postgres"

# -----------------------------------------------------------------------------
# Next.js
# -----------------------------------------------------------------------------
NEXT_PUBLIC_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# JWT — REQUIRED
# -----------------------------------------------------------------------------
# Generate a secret:  openssl rand -base64 32
JWT_SECRET="CHANGE_ME"

# -----------------------------------------------------------------------------
# Polar SDK (optional — future monetization)
# -----------------------------------------------------------------------------
# https://sandbox.polar.sh/dashboard  or  https://polar.sh/dashboard
POLAR_ACCESS_TOKEN="polar_at_sandbox_your_access_token_here"
POLAR_WEBHOOK_SECRET="whsec_your_webhook_secret_here"
POLAR_ORGANIZATION_ID="org_your_organization_id_here"

# Product IDs for tier mapping (find these in Polar Dashboard → Products)
POLAR_PRODUCT_ID_BASIC="prod_your_basic_product_id_here"
POLAR_PRODUCT_ID_PREMIUM="prod_your_premium_product_id_here"

# -----------------------------------------------------------------------------
# CORS (optional — for cross-origin deployment)
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins, or * to allow all
# Example: ALLOWED_ORIGINS=http://localhost:3002,https://yourdomain.com
# ALLOWED_ORIGINS=http://localhost:3002



OPENAI_TIMEOUT_MS=30000